# ── 공통 의존성 (플랫폼 무관) ──────────────────────────────
# torch / torchaudio / demucs 는 플랫폼마다 별도 설치 필요.
# setup-win.bat (Windows CUDA) 또는 setup-mac.sh (Mac CPU) 실행.

fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
eval_type_backport==0.2.0
python-multipart==0.0.12
httpx==0.27.0

# 오디오 분석 (플랫폼 공통)
numpy==1.26.4
scipy==1.13.1
librosa==0.10.2
soundfile==0.12.1

# Replicate (프로덕션 GPU 호출 — 플랫폼 무관)
replicate==1.0.0

# 스토리지
supabase==2.9.0
